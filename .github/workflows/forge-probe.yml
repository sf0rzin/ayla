name: Forge runner probe

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  probe:
    runs-on: windows-latest
    timeout-minutes: 5
    steps:
      - name: Confirm runner execution
        shell: pwsh
        run: Write-Output "Forge runner is executing GitHub Actions jobs."
